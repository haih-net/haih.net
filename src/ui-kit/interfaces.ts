/**
 * Common size enum for UI components
 */
export enum ComponentSize {
  SM = 'sm',
  MD = 'md',
  LG = 'lg',
}

/**
 * Common variant enum for UI components
 */
export enum ComponentVariant {
  DEFAULT = 'default',
  PRIMARY = 'primary',
  SECONDARY = 'secondary',
  SUCCESS = 'success',
  WARNING = 'warning',
  DANGER = 'danger',
}
